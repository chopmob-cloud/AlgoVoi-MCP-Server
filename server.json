{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.chopmob-cloud/algovoi-mcp-server",
  "description": "AlgoVoi crypto payments via MCP: USDC + native on Algorand, VOI, Hedera, Stellar, Base, Solana, Tempo. 29 tools incl. compliance trust queries; selectable substrate/standard response model. ATB-Tested (agent-trust-bench.algovoi.co.uk).",
  "repository": {
    "url": "https://github.com/chopmob-cloud/AlgoVoi-Platform-Adapters",
    "source": "github"
  },
  "version": "1.7.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@algovoi/mcp-server",
      "version": "1.7.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "ALGOVOI_API_KEY",
          "description": "Your AlgoVoi API key (algv_...) from https://dash.algovoi.co.uk.",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "ALGOVOI_API_BASE",
          "description": "AlgoVoi API base URL. Default https://cloud.algovoi.co.uk (managed payouts); use https://api.algovoi.co.uk for direct API access.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "https://cloud.algovoi.co.uk"
        },
        {
          "name": "ALGOVOI_TENANT_ID",
          "description": "Tenant UUID. Required only for direct API access. Leave unset when using AlgoVoi Cloud.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ALGOVOI_PAYOUT_ADDRESS",
          "description": "Default payout wallet address. Required only for direct API access; managed in Cloud otherwise.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ALGOVOI_MODE",
          "description": "Response model: 'substrate' (default — AlgoVoi signed compliance/settlement receipts, action_ref, composite trust hash) or 'standard' (bare x402/MPP/AP2 shape, substrate keys stripped).",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "substrate"
        },
        {
          "name": "ALGOVOI_WEBHOOK_SECRET",
          "description": "Optional HMAC-SHA256 secret for webhook verification. Enables the verify_webhook tool.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        }
      ]
    },
    {
      "registryType": "pypi",
      "identifier": "algovoi-mcp",
      "version": "1.7.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "ALGOVOI_API_KEY",
          "description": "Your AlgoVoi API key (algv_...) from https://dash.algovoi.co.uk.",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "ALGOVOI_API_BASE",
          "description": "AlgoVoi API base URL. Default https://cloud.algovoi.co.uk (managed payouts); use https://api.algovoi.co.uk for direct API access.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "https://cloud.algovoi.co.uk"
        },
        {
          "name": "ALGOVOI_TENANT_ID",
          "description": "Tenant UUID. Required only for direct API access. Leave unset when using AlgoVoi Cloud.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ALGOVOI_PAYOUT_ADDRESS",
          "description": "Default payout wallet address. Required only for direct API access; managed in Cloud otherwise.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ALGOVOI_MODE",
          "description": "Response model: 'substrate' (default — AlgoVoi signed compliance/settlement receipts, action_ref, composite trust hash) or 'standard' (bare x402/MPP/AP2 shape, substrate keys stripped).",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "substrate"
        },
        {
          "name": "ALGOVOI_WEBHOOK_SECRET",
          "description": "Optional HMAC-SHA256 secret for webhook verification. Enables the verify_webhook tool.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        }
      ]
    }
  ]
}
